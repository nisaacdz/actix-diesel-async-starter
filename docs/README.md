# Actix Web Starter with Diesel Async

A ride-hailing backend for Ghana's intercity buses and intracity troskis, built with **Actix Web**, **Diesel**.

## Getting Started

1. Configure `.env` using the .env.example as template
2. Run `diesel migration run`
3. Run `cargo run`
