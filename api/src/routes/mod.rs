use actix_web::web::ServiceConfig;

pub fn configure_routes(_cfg: &mut ServiceConfig) {
    // Configure API routes here
}

pub fn configure_cron_routes(_cfg: &mut ServiceConfig) {
    // Configure Cron routes here
}
